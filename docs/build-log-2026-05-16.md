# Build Log — 2026-05-16

**Project:** SedonaCRM — Anderson Commercial Properties  
**Branch:** main  
**Session model:** claude-sonnet-4-5 (set via /model this session)

---

## Environment

- `cc` alias added to `~/.bashrc` → launches `claude` from any directory
- Model set to `claude-sonnet-4-5` (Sonnet 4.5) for this session and forward

---

## Issues Module — Refinements

### Commits
- `bb02aba` Issues: stacked property filters, remove all inline editing
- `176e9c2` Issues module: layout cleanup, filter consolidation, date format, Kanban fix
- `9a1dc98` Issues module: kanban view, inline editing, FU date column, date filters, priority counts, clear filters, stat chips
- `90ea8f1` Add Next.js routing for Issues module — template for all future modules
- `c8e98a5` IssuesView: sort, ctrl+click focus, back nav state, property quick-filter

### What was built / changed

**Kanban view**
- Toggle between Table and Kanban view (≡ / ⊞ buttons, top right)
- Kanban columns: ??? | Urgent | High | Medium | Low
- Drag and drop between columns saves `priority` to Supabase immediately (optimistic update, rollback on failure)
- Cards show: title (2-line clamp), prop code, category, assigned user first name, FU date (amber + ⚠ if overdue), status badge

**Inline editing removed**
- All inline editing stripped from the table — rows are click-to-open only
- Detail view retains `EditableField` for all editable fields

**Stacked property filters**
- Row 1 (above all other filters): scrollable horizontal strip of active property buttons (All + each active prop_code)
- Multi-select: clicking a prop toggles it; All clears the filter
- When ≥1 prop selected, table switches to grouped view with sticky property header rows showing count

**Filter bar (Row 2)**
- Priority filter: All | ??? | Urgent | High | Medium | Low — each shows live count of matching records (excluding the priority filter itself, so counts reflect all other active filters)
- Status filter: Open | Closed | All
- More… button: popover with Assigned filter (All / Scott / Gabrielle / None) and date filters (Opened / Updated / Closed × This Week / This Month / This Year). Only one date period active at a time. More… border turns amber when any filter inside is active.
- Clear Filters button: appears (orange) only when any filter is active; resets all filters to defaults

**Search box**
- Searches issue_name, prop_code, category, issue_details, assigned user full_name

**Date format**
- All date columns: MM-DD-YYYY numeric format
- FU Date: amber color + ⚠ prefix if overdue and issue is not Closed

**Column renames / reorder**
- Issue Title | FU Date | Prop | Priority | Assigned | Status | Updated | Opened | Closed
- Type column removed
- All column headers sortable; click toggles asc/desc

**Stat chips removed**
- Removed the summary stat boxes (Open / Urgent / Overdue counts) that appeared above the table — cleaner header

**Routing (template for all future modules)**
- `pages/issues/index.jsx` — list page wraps IssuesView in AppShell
- `pages/issues/[id].jsx` — cold-loadable detail page fetches by UUID
- `components/AppShell.jsx` — shared sidebar/chrome; Issues nav → `/issues`
- SedonaCRM.jsx Issues nav → `router.push('/issues')`
- Back button uses `sessionStorage.getItem('issuesBackUrl') || '/issues'`
- Escape key closes detail (unless focus is in an input/textarea)
- Ctrl+click row → `window.open('/issues/${id}', '_blank')`

---

## Work Orders Module — Full Rebuild from Issues Template

### Commits
- `95f6547` Work Orders: rebuilt from Issues template with kanban, stacked filters, priority counts, date filters popover, clear filters
- `d7edc96` Work Orders: WO# col, Stage/Status split, Vendor/Tenant cols, priority kanban, anchor routing, /work-orders pages

### Schema confirmed before building
```
Table: work_orders
Key fields used:
  podio_wo_number  integer   — WO # (all 2914 rows populated; wo_num always null)
  short_description text     — WO Title
  follow_up_date   date      — FU Date
  prop_code        text      — Prop
  priority         text      — same values as Issues: ???/Urgent/High/Medium/Low
  wo_status        text      — Stage (New/Request Sent/Re-Opened/Closed/Closed-Not Done)
  status           text      — empty in all rows (ignored)
  stage            text      — empty in all rows (ignored)
  vendor_id        uuid      → vendors.company_dba
  tenant_id        uuid      → tenants.tenant_dba (135/2914 rows have tenant)
  created_at       timestamptz — Opened
  updated_at       timestamptz — Updated
  closed_at        date       — Closed
  close_date       date       — alternate closed field
```

### Table columns (12 total)
`WO # | WO Title | FU Date | Prop | Priority | Stage | Status | Vendor | Tenant | Updated | Opened | Closed`

- **WO #** — `podio_wo_number`; sortable; included in search; real `<a>` anchor (see routing fix below)
- **WO Title** — `short_description`; auto width
- **FU Date** — `follow_up_date`; amber + ⚠ if overdue and not closed
- **Prop** — `prop_code`; accent color
- **Priority** — `priority` with PriorityDot color indicator
- **Stage** — `wo_status` badge (New=blue / Request Sent=purple / Re-Opened=amber / Closed=green / Closed-Not Done=red)
- **Status** — derived open/closed badge from `isWoClosed(wo)`: Open (blue) / Closed (muted); neither column truncates
- **Vendor** — `vendor_id → vendors.company_dba`; truncation OK; tooltip on hover
- **Tenant** — `tenant_id → tenants.tenant_dba`; truncation OK; tooltip on hover
- **Updated / Opened / Closed** — timestamps in MM-DD-YYYY format

### Kanban
- Columns by Priority: ??? | Urgent | High | Medium | Low (same as Issues)
- Drag saves `priority` to Supabase immediately (optimistic, rollback on failure)
- Cards show: WO title (2-line clamp), prop code, WO # (`#3576`), stage badge, vendor (truncated), FU date (amber if overdue)

### Ctrl+click / middle-click routing fix
- **Root cause:** no `pages/work-orders/` directory existed → all deep-link URLs 404'd
- **Fix:** WO # cell wraps a real `<a href="/work-orders/{id}">` anchor
  - Regular click: `e.preventDefault()` + SPA navigation via `onSelect(wo)`
  - Ctrl+click / middle-click / right-click → "Open in new tab": browser follows href natively
  - `<tr>` onClick uses `e.target.closest('a')` guard so anchor clicks don't double-fire

### Routing pages created
- `pages/work-orders/index.jsx` — list page; wraps WorkOrdersView in AppShell
- `pages/work-orders/[id].jsx` — fetches WO by UUID, renders WorkOrderDetail in AppShell; back button → `sessionStorage.getItem('workOrdersBackUrl') || '/work-orders'`

### AppShell update
- Work Orders nav updated from `/?view=work-orders` → `/work-orders`

### Filters (identical to Issues)
- Stacked property buttons (Row 1), priority counts, status filter, More… date popover, Clear Filters (orange when active), search box
- Search covers: podio_wo_number, short_description, prop_code, category, internal_notes, vendor name, tenant name
- No Assigned filter (no user assignment field on work_orders; vendor is the closest analogue)
- Open/Closed logic: `wo_status` in ['Closed', 'Closed - Not Done'] = closed; everything else = open

### No inline editing
- All table rows are read-only click-to-open
- Detail view (WorkOrderDetail) is fully read-only (no EditableField)

### Detail view tabs
- **Info**: WO Number, Title, Category, WO Type, Priority, Stage + Dates card + Instructions/Details card
- **Notes**: Internal Notes (read-only pre-wrap display)
- ActivityPanel (resizable, collapsible) on right side — placeholder until Podio sync at go-live

---

## Files Changed This Session

| File | Change |
|------|--------|
| `components/IssuesView.jsx` | Kanban, filter bar, stacked props, inline editing removed, column renames |
| `components/WorkOrdersView.jsx` | Full rebuild from Issues template |
| `components/AppShell.jsx` | Work Orders nav → `/work-orders` |
| `pages/issues/index.jsx` | New — list routing page |
| `pages/issues/[id].jsx` | New — detail routing page |
| `pages/work-orders/index.jsx` | New — list routing page |
| `pages/work-orders/[id].jsx` | New — detail routing page |

---

## Next Priorities

1. Build complete Property detail with all 5 tab groups using lazy-loaded reusable components
2. Wire Home dashboard urgent items to real data
3. Build Leasing Pipeline standalone view (using same Issues/WorkOrders template)
